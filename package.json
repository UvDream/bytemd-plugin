{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "rm -rf packages/*/{lib,dist,tsconfig.tsbuildinfo}",
    "dev": "tsc --build --watch"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {}
}